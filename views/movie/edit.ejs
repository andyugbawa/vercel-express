<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        body{
            background: linear-gradient(30deg,#3e0219 20%,#4d1472 40%,#13011c 40% );
        }
        .edit-container{
            display: flex;
            justify-content: center;
        }
        h1{
            color:blue
        }
        .title-name{
            color:white;
        }
        .genre-name{
            color:white;
        }
        .year-name{
            color:white;
        }
        input{
            width: 920px;       
            padding: 8px;      
            font-size: 16px;    
        
        }
        .year{
            margin-top: 30px;
        }
        .genre{
            margin-top: 30px; 
        }
        .all-btn{
            color:black;
        }
        .back-btn{
            color:black;
        }

        .invalid-input {
           border: 2px solid red;
           background-color: #ffe6e6;
       }
       .valid-input {
           border: 2px solid green;
           background-color: #e6ffe6;
       }
       .submit{
        padding: 30px 0 0 0;
       }
       .submit-btn{
        padding: 10px 20px 10px 20px;
        border-radius: 15px;
        font-weight: bold;
        font-size: 15px; 
        background: linear-gradient(to bottom, #4CAF50, #1e1717);
        
       }
       .reverse-btn{
        display: flex;
        padding: 30px 0 0 0;
        gap: 10px;
       }
       .btn-content{
        display: flex;
        gap: 20px;
       }
       .movie-all{
        padding: 5px 20px 10px 20px;
        border-radius: 15px;
        font-weight: bold;
        font-size: 15px;
        background: linear-gradient(-45deg, #e64307 90%, #eae5e5 5%);
        

       }
       .back-movie{
        padding: 5px 20px 10px 20px;
        border-radius: 15px;
        font-weight: bold;
        font-size: 15px;
        background: linear-gradient(-45deg,rgb(8, 8, 220) 90%,rgb(160, 178, 196) 5%);
       }
                 
       @media (max-width:768px) {
            input{
            width: 350px;       
            padding: 8px;      
            font-size: 16px;    
        
        }
        }

                
        @media (max-width:500px) {
            input{
            width: 250px;       
            padding: 8px;      
            font-size: 16px;    
        
        }
        }
   </style>
</head>
<body>

    <div class="edit-container">
        <div class="edit-content">
            <h1>EDIT PAGE</h1>
            <form id="movieForm"  action="/movie/<%= movie._id %>?_method=PUT" method="POST"  >
                <div>
                    <div class="title">
                        <label class="title-name" for="title">Title</label>
                    </div>
                    <input type="text" name="movie[title]" id="title" placeholder="title" value="<%=movie.title %>" required>
                </div>
               
               
                <div>
                    <div class="genre">
                        <label class="genre-name" for="genre">Genre</label>
                    </div>
                    <input  type="text" name="movie[genre]" id="genre" placeholder="genre" value="<%=movie.genre %>" required>
                </div>
                <div>
                    <div class="year">
                        <label class="year-name" for="year">Year</label>
                    </div>
                    <input type="number" name="movie[year]" id="year" placeholder="year" value="<%=movie.year %>" required >
                </div>
                <div class="btn-content">
                    <div class="submit">
                        <button class="submit-btn">Submit</button>
                    </div>
                     <div class="reverse-btn">
                        <button class="movie-all">
                            <a  class="all-btn" href="/movie">ALL MOVIES</a>
                        </button>
                        <button class="back-movie">
                            <a  class="back-btn" href="/movie/<%= movie._id %>">Back to movies</a>
                        </button>
                     </div>
                </div>
            </form>
    
        </div>

    </div>


        
    <script>
        (function () {
            'use strict'

            const form = document.getElementById('movieForm');
            const inputs = form.querySelectorAll('input');

            form.addEventListener('submit', function (event) {
                let isValid = true;

                inputs.forEach(input => {
                    if (!input.value.trim()) {
                        // Highlight invalid input
                        input.classList.add('invalid-input');
                        input.classList.remove('valid-input');
                        isValid = false;
                    } else {
                        // Highlight valid input
                        input.classList.remove('invalid-input');
                        input.classList.add('valid-input');
                    }
                });

                if (!isValid) {
                    event.preventDefault(); // Prevent form submission if invalid
                }
            });

            // Optional: Real-time validation on input
            inputs.forEach(input => {
                input.addEventListener('input', function () {
                    if (input.value.trim()) {
                        input.classList.remove('invalid-input');
                        input.classList.add('valid-input');
                    } else {
                        input.classList.remove('valid-input');
                        input.classList.add('invalid-input');
                    }
                });
            });
        })();
        
    </script>

   
</body>
</html>